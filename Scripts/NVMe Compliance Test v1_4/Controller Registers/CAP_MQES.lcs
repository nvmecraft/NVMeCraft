NVMeDevice dev;
Serilog.Core.Logger log;

public int ScriptMain(int a, int b)
{
    dev = FromEngine.dev;
    log = FromEngine.log;
    
    log.Information("CAP.MQES - Test Start");
    
    UInt64 CAP_MQES = dev.controllerRegs.CAP.MQES;
    log.Information("CAP.MQES - 0x{0:X2}", CAP_MQES);
    
    
    if(CAP_MQES < 0x01)
    {
        log.Warning("CAP.MQES - The minimum value is 1h.");
    }
    
    log.Information("CAP.MQES - Test Done");
    
    return 0;
}